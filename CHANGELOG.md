# Changelog

## 0.9.0 — 2026-07-28

데이터 용량이 아니라 **부분 설치·부분 수집·게시 중단에도 마지막 정상본을
유지하는 것**을 목표로 한 무결성·운영 보강 릴리스입니다. 이전에 0.9.0으로
예고했던 문서 검증 기능은 이번 릴리스에 포함하지 않습니다.

### 확장프로그램 데이터 무결성

- IndexedDB v2의 비파괴 A/B snapshot 구조를 도입했습니다. 기존 `cases`
  저장소는 보존되며, 새 snapshot을 staging·전수 검증한 뒤 활성 포인터만
  원자적으로 전환합니다.
- 모든 동기화 trigger를 single-flight로 합쳐 동일 fetch·검증·설치 결과를
  공유합니다.
- manifest version·건수·SHA-256과 content-addressed 객체를 검증하고,
  무조건부 304·downgrade·불완전 bundle 활성화를 차단합니다.
- 조회 데이터와 법원 메타데이터를 동일 snapshot transaction에서 읽습니다.

### 크롤러·게시·복구

- XML·필수 필드·페이지 완결성을 날짜 단위로 검증하고, 부분 실패를 durable
  backlog에 남겨 다음 실행에서 재시도합니다.
- backlog가 남은 부분 수집 결과는 공개하지 않고 last-known-good DB를
  유지합니다.
- SQLite state는 generation 객체와 `current`/`previous` 포인터로 게시하며,
  최신 세대가 손상되면 완전한 이전 세대로만 복구합니다.
- 공개 DB는 immutable 객체 → manifest → 구버전 mirror 순서로 게시합니다.

### 보안·품질·공개 문구

- GitHub Actions secret을 필요한 step으로 한정하고 공식 Action과 production
  의존성을 commit/digest/hash로 고정했습니다.
- WARP private key를 저장소에서 제거하고 WARP step 전용 GitHub secret으로
  외부화했습니다.
- 개인정보처리방침·스토어 설명을 실제 보조 네트워크 요청과 배지 의미에 맞게
  정정했습니다.
- ESLint, Ruff, Jest, Python unittest, Playwright E2E를 CI gate에 연결했습니다.

## 0.8.2 — 2026-06-17

0.8.1에서 drift 안전망이 동기화를 자주 돌리며 생긴 회귀("검증이 느려지고
초록 배지가 주황으로 바뀜")를 잡는 핫픽스 릴리스.

### 회귀 수정

- **비파괴 DB 재구축**: 재동기화가 기존 DB를 비우고 다시 채우던 방식(빈 DB
  노출 창)을 덮어쓰기 삽입 → 이전 키만 가지치기 방식으로 전환. 동기화
  진행 중에도 조회가 빈/부분 DB를 보지 않습니다.
- **룩업 타임아웃의 오탐 제거**: 배치 조회가 느리거나(콜드 스타트 ~20초)
  일시적으로 실패할 때, 진짜 판례를 "DB 미확인(주황)"으로 단정하던 동작을
  수정. 확정 전까지 표시를 미루고 재조회로 늦게 온 응답을 반영합니다.

### Gemini · Copilot · Grok 동작 안정성 보강

- **깜빡임 제거**: 일부 서비스는 응답 생성 중 화면을 계속 다시 그려 표시한
  배지가 지워졌다 다시 그려지길 반복했습니다. 응답이 멈춘 뒤 한 번에 표시하도록
  바꿔 깜빡임을 없앴습니다.
- **표시 지연 해결**: 사건번호가 한 줄에 여러 개 출력되는 경우(예: Gemini),
  한 개씩만 표시되며 모두 뜨기까지 한참 걸리던 문제를 수정 — 이제 한 번에
  모두 표시됩니다.

### 사건번호 복사 UX 개선

- 단축키(Alt+C)가 일부 서비스의 자체 단축키에 가로채여 동작하지 않던 문제를
  보완했습니다. 미확인 사건번호 안내 배너에 **'사건번호 복사' 버튼**과
  **닫기(×) 버튼**을 추가했습니다. (버튼 클릭은 클립보드 복사가 더 안정적입니다.)
- 배너는 5초 후 자동으로 닫히며, 복사하면 '복사됨' 표시 뒤 3초 후 닫힙니다.
- 팝업에 **'미확인 사건번호 팝업 표시'** 토글을 추가해 안내 배너를 끌 수 있습니다.

### 정확성

- **2자리 연도 해석 상대화**: 하드코딩 기준(2030년부터 깨짐)을 현재연도 기준
  슬라이딩 윈도우로 교체 — 시간이 지나도 정상 사건을 오탐하지 않습니다.
- **헌재 연도 하한**: 헌법재판소(1988 설립) 이전 연도의 헌재 사건번호를
  형식 오류(빨강)로 판정합니다.
- **숫자열 오탐 차단**: 연도 앞에 숫자가 붙은 경우(전화번호·계좌번호 등)를
  사건번호로 잘못 인식하던 문제, 그리고 비현실적 4자리 연도(예: 3015)가
  2자리로 잘려 미래연도 검출을 우회하던 문제를 수정했습니다.

### 권한 축소

- 전 탭의 URL을 읽을 수 있는 `tabs` 권한을 제거하고, 배지 표시에 필요한
  **지원 LLM 사이트(+ law.go.kr / api.bup.live)로 한정된 호스트 권한**으로
  대체했습니다. 다른 사이트의 방문 기록은 읽지 않습니다. (프라이버시 문서 정정)

### 정리

- 서버·파이프라인에서 제거된 조세(조심)·특허(KIPRIS) DLC의 클라이언트 탐지
  로직과 문서를 실제 동작에 맞춰 정리했습니다.
- pre-push 훅이 잘못된 경로로 단위 테스트를 스킵하던 문제를 수정 (릴리스
  게이트 복구).

## 0.8.1 — 2026-06-07

0.8.0에서 일부 사용자의 DB가 약 2달간 갱신되지 않던 사고(재설치로만 복구 가능,
팝업 새로고침 무력)의 재발 방지에 집중한 핫픽스 릴리스.

> 문서 검증(Intelligent Document Reader)은 안정화를 위해 0.9.0으로 보류.

### 동기화 견고성 (S1)

- **정직한 결과 보고**: 동기화 실패가 더 이상 성공으로 위장되지 않습니다.
  팝업 새로고침 버튼이 실패 시 사유와 함께 알림을 띄웁니다.
- **304 가드**: 로컬 DB가 비정상(빈 DB, 부분 삽입 잔해)이면 저장된 ETag를
  신뢰하지 않고 전체 DB를 다시 받습니다 — "etag는 있는데 DB는 빈" 트랩 해소.
- **48시간 워치독**: 마지막 성공 동기화가 48시간을 넘기면 조건부 요청을
  생략하고 무조건 최신 DB를 받습니다.
- **다운그레이드 금지**: 동기화 실패가 최신 로컬 DB를 구버전 번들로
  덮어쓰지 않습니다 (빈 DB 부트스트랩 또는 번들이 더 최신일 때만 로드).
- **fetch 타임아웃(60s)**: 응답 없는 연결이 동기화를 무기한 정지시키지 않습니다.
- **동기화 원장**: 모든 동기화 시도(트리거·결과·소요시간)가 기록되며,
  팝업의 "동기화 이력" 섹션에서 확인할 수 있습니다.

### Drift 안전망 (S2)

- 서버가 게시하는 manifest(정답지)와 로컬 DB의 버전·해시·건수를 대조해
  뒤처짐이 확정되면 캐시 버스터로 강제 재동기화하는 자가치유를 추가했습니다.
- LLM 사이트 진입 시 신선도 검사가 발화합니다 (4시간 스로틀, 치유 실패
  3회 시 백오프, manifest 미게시/불량 시 무동작 fail-open).

### 파이프라인 (S3)

- 크롤러가 DB 업로드 후 `manifest.json`(버전·sha256·건수)을 마지막에
  게시합니다 (no-cache). 업로드 순서가 "manifest는 최신인데 DB는 구버전"
  레이스를 차단합니다.

### 기타

- 번들 DB를 20260321(92,425건) → 20260603(130,778건)으로 갱신.
- Core DB 교체 시 키 범위 일괄 삭제로 전환 — 10만건 이상에서 수십 초
  걸리던 교체가 1초 미만으로 단축.
- 업그레이드 효과: 기존 0.8.0에서 정체된 클라이언트는 이번 업데이트 직후
  첫 동기화에서 성공 이력 부재(`no_success_record`)로 판정되어 자동으로
  전체 DB를 다시 받습니다 — 별도 조치 불필요.
