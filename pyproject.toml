[tool.ruff]
target-version = "py310"

[tool.ruff.lint]
# Focus on syntax/undefined-name/import errors; formatting and broad style rewrites
# are intentionally outside this maintenance change.
select = ["E4", "E7", "E9", "F"]
